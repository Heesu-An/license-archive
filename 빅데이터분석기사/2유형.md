
템플릿이라고 하긴 좀 애매할 수 있는데, 여러 강의랑 코드를 통해 아래와 같이 흐름 잡았다.

-   **회귀**: RandomForestRegressor, XGBRegressor, LGBMRegressor 모델로 학습하고, RMSE, MAE, R2 score로 평가
-   **분류**: RandomForestClassifier, XGBClassifier, SVC 모델로 학습하고, roc\_auc\_score, accuracy\_score로 평가

<br>

## 

전체 흐름은 아래와 같다. **4~5 부분만 회귀, 분류에 따라 다르고 나머지 단계들은 공통**이다.

<br>

> 1\. 데이터 유형 파악  
> 2-1. 데이터 드랍 및 변수에 할당  
> 2-2. 전처리 (결측치, 이상치, 범주명 통일)  
>     - 수치형, 범주형 컬럼 리스트  
>     - 각 범주형 변수의 카테고리별 개수 및 훈련, 테스트 데이터 카테고리 종류 같은지 확인  
>     - 각 수치형 변수 이상치 확인 (하면 좋은데 이 부분은 사실상 안함 - 투머치라고 생각함)  
>     - (필요시) 결측값 처리  
> 2-3. 수치형 변수 스케일링  
> 2-4. 범주형 변수 인코딩  
> 3\. 데이터 분할  
> **4\. 모델 학습 -> (회귀/분류)에 따라 다름**  
> **5\. 평가 **\-> (회귀/분류)에 따라 다름****  
> 6\. 5의 결과, 성능 가장 좋은 모델로 테스트 데이터 결과 예측 및 저장  
> 7\. 저장한 결과 다시 불러와서 확인 (문제 조건에 따라 잘 저장했는지 확인)


<br>


## **분류 & 회귀 공통 부분 (1-3, 6-7)**

```
import pandas as pd

### 1. 데이터 유형 파악
print(X_train.info())
print(y_train.info())
print(X_test.info())
print(y_test.info())
print(X_train.head())
print(y_train.head())


### 2-1. 데이터 할당 및 드랍
X_train = X_train.drop(["CustomerId", "Surname"], axis=1)
y_train = y_train.drop("CustomerId", axis=1)
X_test = X_test.drop(["CustomerId", "Surname"], axis=1)
y_test = y_test.drop("CustomerId", axis=1)
print(X_train.shape, y.shape, X_test.shape)


### 2-2. 전처리 (결측치, 이상치, 불균형, 범주명 조정)
# 수치형, 범주형 컬럼 리스트
not_num_col = X_train.select_dtypes(include="object").columns
num_col = X_train.select_dtypes(exclude="object").columns

# 각 범주형 변수의 카테고리별 개수 및 훈련, 테스트 데이터 카테고리 종류 같은지 확인
for not_num_col in not_num_cols:
    print()
    print(X_train[not_num_col].value_counts())
    print(set(X_train[not_num_col]), set(X_test[not_num_col]))
X_train["Gender"] = X_train["Gender"].apply(lambda x: x.lower().strip())
X_test["Gender"] = X_test["Gender"].apply(lambda x: x.lower().strip())

# (필요시) 각 수치형 변수 확인 및 정리 (이상치 제거, 결측치 처리)
X_train['결측치 열'] = X_train['결측치 열'].fillna(0)
X_test['결측치 열'] = X_test['결측치 열'].fillna(0)
print(X_train.isnull().sum())
print(X_test.isnull().sum())



### 2-3. 수치형 변수 스케일링
from sklearn.preprocessing import MinMaxScaler
scaler = MinMaxScaler()
X_train[num_col] = scaler.fit_transform(X_train[num_col])  # 스케일링
X_test[num_col] = scaler.transform(X_test[num_col])   # 스케일링 한 것을 적용


### 2-4. 모든 범주형 변수 인코딩
from sklearn.preprocessing import LabelEncode
for col in not_num_col:  # 각 카테고리 다를 때, 카테고리 개수 많은 걸로!
    encoder = LabelEncoder()
    X_train[col] = encoder.fit_transform(X_train[col])
    X_test[col] = encoder.transform(X_test[col])

y_encoding = False
if y_train.dtype == 'object':
    y_encoding = True
    y_encoder = LabelEncoder()
    y_train = y_encoder.fit_transform(y_train)


### 3. 데이터 분리
from sklearn.model_selection import tran_test_split
X_train, X_val, y_train, y_val = train_test_split(X_train, y, test_size=0.2)   # 🚨🚨🚨🚨 입력 및 출력 순서
print(X_train.shape, X_val.shape, y_train.shape, y_val.shape)

.
.
.

### 6. 5의 결과, 성능 가장 좋은 모델로 테스트 데이터 결과 예측 및 저장
y_pred = model.predict(X_test)
if y_encoding:   # 인코딩 되돌려서 저장
    y_pred = y_encoder.inverse_transform(random_pred)
result = pd.DataFrame(y_pred, columns=['pred'])
result.to_csv('result.csv', index=False)

rmse = round(np.sqrt(mean_squared_error(y_test, y_pred)), 3)
r2 = round(r2_score(y_test, y_pred), 3)
print("gbm", rmse, r2)


### 7. 생성 결과 확인
result = pd.read_csv('result.csv')
print(result)
print(y_test)
```


<br>


## **회귀 (4~5)**

```
### 4. 모델 학습
from sklearn.ensemble import RandomForestRegressor
from xgboost import XGBRegressor
from lightgbm import LGBMRegressor
random_model = RandomForestRegressor()
random_model.fit(X_train, y_train)
random_pred = random_model.predict(X_val)

xgb_model = XGBRegressor()
xgb_model.fit(X_train, y_train)
xgb_pred = xgb_model.predict(X_val)

gbm_model = LGBMRegressor()
gbm_model.fit(X_train, y_train)
gbm_pred = gbm_model.predict(X_val)


### 5. 평가
from sklearn.metrics import mean_squared_error, r2_score, mean_absolute_error
import numpy as np
random_rmse = round(np.sqrt(mean_squared_error(y_val, random_pred)), 3)
random_r2 = round(r2_score(y_val, random_pred), 3)
random_mae = round(mean_absolute_error(y_val, random_pred), 3)
xgb_rmse = round(np.sqrt(mean_squared_error(y_val, xgb_pred)), 3)
xgb_r2 = round(r2_score(y_val, xgb_pred), 3)
xgb_mae = round(mean_absolute_error(y_val, random_pred), 3)
gbm_rmse = round(np.sqrt(mean_squared_error(y_val, gbm_pred)), 3)
gbm_r2 = round(r2_score(y_val, gbm_pred), 3)
gbm_mae = round(mean_absolute_error(y_val, random_pred), 3)
print("랜덤", random_rmse, random_r2)
print("xgb", xgb_rmse, xgb_r2)
print("gbm", gbm_rmse, gbm_r2)
```

<br>



## **분류 (4~5)**

```
### 4. 모델 학습
from sklearn.ensemble import RandomForestClassifier
from xgboost import XGBClassifier
from sklearn.svm import SVC
random_model = RandomForestClassifier()
random_model.fit(X_train, y_train)
random_pred = random_model.predict(X_val)

xgb_model = XGBClassifier()
xgb_model.fit(X_train, y_train)
xgb_pred = xgb_model.predict(X_val)

svc_model = SVC()
svc_model.fit(X_train, y_train)
svc_pred = svc_model.predict(X_val)


### 5. 평가
from sklearn.metrics import roc_auc_score, accuracy_score
random_roc = roc_auc_score(y_val, random_pred)
random_acc = accuracy_score(y_val, random_pred)
xgb_roc = roc_auc_score(y_val, xgb_pred)
xgb_acc = accuracy_score(y_val, xgb_pred)
svc_roc = roc_auc_score(y_val, svc_pred)
svc_acc = accuracy_score(y_val,svc_pred)
print("랜덤:", random_roc, random_acc)
print("xgb:", xgb_roc, xgb_acc)
print("svc:", svc_roc, svc_acc)
```

