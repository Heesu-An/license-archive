
## **공부순서**



빅분기 실기는 문제 얻기가 좀 어려웠다. 기출도!...! 데이터마님으로 다했다 최고!!!! 감사합니다아

3일 공부지만 하루 종일을 공부했다!! 꽤 많은 문제를 풀었다! 아래 순서대로!! 1유형 -> 2유형 -> 3유형 -> 전체

1.  전체적인 내용 파악: 아답터 강의
2.  [데이터 마님 1유형 100제](https://www.datamanim.com/dataset/99_pandas/pandasMain.html) 풀기: 이걸로 1유형 커버 가능
3.  [데이터 마님 2유형 연습문제](https://www.datamanim.com/dataset/03_dataq/typetwo.html) (분류 2문제, 회귀 2문제): 몇 문제만 풀면서 손 익히기
4.  블로그, 유튜브 보면서 3유형 공부 및 내용 정리
5.  [데이터 마님 사이파이 튜토리얼 문제 다 풀기 (3유형)](https://www.datamanim.com/dataset/97_scipy/scipy.html)
6.  [데이터 마님 모의고사 1회](https://www.datamanim.com/dataset/practice/q1.html), [2회](https://www.datamanim.com/dataset/practice/q2.html) 풀기

<br>


## **팁**


함수명이나 파라미터 등이 기억 안날 때는 아래 사용하면 된다. 사용법 좀 익혀야 할 듯.

몇 개 없는데 쓸 일이 있을까 싶었는데, 몰랐던 것들 나왔을 때나 실제로 기억 안나서 아래 함수 많이 사용했다.

```
print(help(df.dropna))   # 함수: 파라미터 기억 안날 때
print(dir(sklearn.preprocessing)    # 라이브러리: 함수명 기억 안날 때
```

<br>

---

<br>

### **1유형**

**1\. Getting & Knowing Data**

```
#### 데이터 불러올 때 옵션 (파라미터) ####
sep = "구분자"
encoding = "utf-8", "cp949", "euc-kr", "latin"
index_col = 인덱스로 지정할 컬럼 인덱스

#### 컬럼명 지정 ####
df = df.rename(columns={0:'counts'}

#### 데이터 타입 ####
# 데이터 타입 확인
df.dtypes           # dtype's' : df의 각 컬럼의 데이터 타입 확인
df['열이름'].dtype    # dtype : 특정 열의 데이터 타입 확인
# 열 데이터 타입 전환
df['열이름'].astype('float')  # 🚨🚨

#### 인덱스 ####
df.index                    # 인덱스 구성 확인
df.reset_index(drop=True)   # 인덱스 초기화
df.set_index('ID', inplace=True, drop=True)   # 🚨🚨특정 컬럼을 인덱스로

#### 컬럼 삭제 ####
df.drop("열이름", axis=1, inplace=True)

#### 특정 컬럼 내 유일값 ####
df['열이름'].unique()   # 유일값
df['열이름'].nunique()  # 유일값 개수

#### Box Plot ####
quantile_3 = df['열'].quantile(0.75)
IQR = df['열'].quantile(0.75) - df['열'].quantile(0.25)
```


<br>


**2\. Filtering & Sorting**

```
#### 인덱싱 ####
df['열이름']         # 특정 열 추출
df.열이름            # 특정 열 추출
df.iloc[1]          # 1행 인덱싱
df.iloc[0:2, 0:2]   # 0~1행, 0~1열 인덱싱
df[df["age"] > 20]  # 조건을 만족하는 행 인덱싱

#### 조건 데이터 #### 🚨🚨🚨🚨
df[df['item_name'].str.contains("Chips")]    # item_name 열에서 Chips를 포함하는 경우
df['item_price'].str[1:].astype('float')
df[df['new_price'].isin(리스트)]               # new_price 컬럼에서 리스트 내에 있는 값이라면 추출

#### 컬럼 내 중복 제거 🚨🚨####
df.drop_duplicates("열이름", keep="last" 혹은 "first")  # 열 내 중복 제거. 여러 개 중 첫번째 혹은 마지막 꺼 선택

#### 정렬 ####
df.sort_values("기준열", ascending=True)   # 컬럼 기준 오름차순 정렬
df.sort_index(ascending=True)             # 인덱스 기준 오름차순 정렬

#### 결측치 ####
print(df.isnull())                       # 결측치 여부 True, False
print(df.isnull().sum())                 # 각 컬럼의 결측치 개수
df.loc[df['choice_description'].isnull(), 'choice_description'] = "NoData"  # 🚨🚨🚨🚨 특정 행의 결측값 대체
df.dropna(subset=['열1', '열2'])          # 지정한 열 결측치 삭제 / 지정 안하면 결측치 있는 행 삭제
df.fillna(method="ffill").fillna(method="bfill")     # 직전 값으로 대체. 첫번째 행은 뒤의 값으로 대체
```


<br>


**3. Grouping**

```
#### 그룹핑 #### 🚨🚨🚨🚨
df.groupby("team")["score"].mean()  # 'team'컬럼을 기준으로 같은 그룹끼리 묶고, score 열을 평균냄.
df.groupby("team")["score"].mean().rest_index()   # 'team'컬럼을 인덱스가 아닌 열로 바꿈. (우리가 흔히 아는 데이터프레임과 숫자 인덱스로 변환)
df.groupby("team").filter(lambda x: x["score"].mean() >= 70)  # 그룹의 평균 score가 70 이상인 팀만
df.groupby(["team", "city"])["score"].mean()  # 'team'컬럼 기준으로 나눈 그룹 안에서, city 컬럼 기준으로 또 나눔

#### 컬럼 내 그룹 별 개수 ####
df['AgeState'].value_counts()    # 컬럼 내 카테고리 별 개수
df.groupby("host_name").size().sort_values()   # 🚨🚨🚨🚨 size() 함수: 그룹 내 카테고리 별 개수
df2 = pd.DataFrame(df.groupby("host_name").size().sort_values(ascending=False), columns=['counts'])
pd.DataFrame(df.groupby("host_name").size().sort_values(ascending=False)).rename(columns={0:'counts'})
df.groupby(["neighbourhood_group", "neighbourhood"], as_index=False).size()  # as_index 매개변수!!
df.groupby(['neighbourhood_group', 'neighbourhood'], as_index=False).size().groupby("neighbourhood_group", as_index=False).max()   # 🚨🚨🚨🚨 "neighbourhood_group의 값에 따른 neighbourhood컬럼 값 중 neighbourhood_group그룹의 최댓값들을 출력하라"
df[['neighbourhood_group', 'price']].groupby("neighbourhood_group", as_index=False).agg(['mean', 'var', 'max', 'min'])    # 🚨🚨🚨🚨 "neighbourhood_group 값에 따른 price값의 평균, 분산, 최대, 최소 값을 구하여라"

tmp = df[['neighbourhood_group', 'room_type']].groupby(['neighbourhood_group', 'room_type']).size().unstack()  # 🚨🚨🚨🚨 "55. 데이터중 neighbourhood_group 값에 따른 room_type 컬럼의 숫자를 구하고 neighbourhood_group 값을 기준으로 각 값의 비율을 구하여라"
tmp.loc[:, :] = tmp.values / tmp.values.sum(axis=1).reshape(-1, 1)
print(tmp)


##### 🚨🚨🚨🚨 apply 함수는 기본적으로 axis=0임. df['name'].apply처럼 열 하나에 apply 함수를 적용하는 것은 시리즈에 적용하는 것이므로 우리가 볼 때는 행 단위로 적용하는 것처럼 보임. 그러나 df[['name', 'age']].apply처럼 적용할 때는 열 단위로 적용되므로 행 단위로 진행하고 싶으면 axis 반드시 지정하기
```


<br>


**4\. Time Series**

```
df.Yr_Mo_Dy = pd.to_datetime(df.Yr_Mo_Dy, format="%Y-%m-%d:%I.%M.%S.%P:%H")  # 날짜 타입으로 변경  (datetime64 타입, 형식 지정 가능)
pd.to_datetime("1961-01-01")                                  # 날짜 타입으로 변경  (형식 자동 인식

#### 날짜 내 특정 값 추출 ####
df.Yr_Mo_Dy.dt.year   (month, day, hour, minute, second)      # 시리즈에서 연월일 추출 (year, month, day)
x.year                                                        # x가 datetime64 타입일 때, 연월일 추출
df.Yr_Mo_Dy.dt.dayofweek                                      # 요일 숫자로 반환
df.Yr_Mo_Dy.dt.day_name()                                     # 요일 이름으로 변환

#### 기간 별 나누기 ####
df.groupby(df.Yr_Mo_Dy.dt.to_period(freq="M")).mean()         # "Y", "M", "W", "W-MON", "D", "Q"
df.resample("W").agg(['min','max','mean','std'])              # 인덱스가 datetime 형식일 때, 주 단위로 리샘플링하여 계산 ("A", "AS", "M", "MS", "W", "D", "Q", "QS")

#### 날짜 연산 ####
df.RPT.diff(periods=1)                                          # 1차 차분
df[['RPT','VAL']].rolling(7).mean()                             # 일주일 이동 평균
pd.to_datetime(date, format="%Y-%m-%d") + pd.Timedelta(days=1)  # 하루 더하기
```

| **카테고리** | **속성/메서드** |
| --- | --- |
| **시간 단위** | .dt.hour |
|   | .dt.minute |
|   | .dt.second |
|   | .dt.microsecond |
| **날짜 단위** | .dt.year |
|   | .dt.month |
|   | .dt.day |
| **요일/주차** | .dt.dayofweek |
|   | .dt.day\_name() |
|   | .dt.dayofyear |
|   | .dt.weekofyear (🚨) |
| **기간/분기** | .dt.quarter |
|   | .dt.is\_month\_start |
|   | .dt.is\_month\_end |

**5\. Merge , Concat**

```
- merge: 인덱스, 공통 컬럼을 기준으로 합침
- concat: 갖다 붙임 ---> 추천

#### concat 파라미터 ####
axis = 0(행), 1(열)
join = 'outer'(기본값, 합집합), 'inner'(교집합)

#### concat ####
pd.concat([df1, df2], join='inner')
pd.concat([df1, df2], join='inner', axis=1)

#### merge ####
pd.merge(df1, df2, on='공통열', how='inner')
pd.merge(df1, df2, left_on='df1열', right_on="df2열" how='outer')
```


<br>


---

<br>

### **2유형**

템플릿이라고 하긴 좀 애매할 수 있는데, 나는 여러 강의랑 코드 보고 아래와 같이 흐름 잡았다.

-   **회귀**: RandomForestRegressor, XGBRegressor, LGBMRegressor 모델로 학습하고, RMSE, MAE, R2 score로 평가
-   **분류**: RandomForestClassifier, XGBClassifier, SVC 모델로 학습하고, roc\_auc\_score, accuracy\_score로 평가

전체 흐름은 아래와 같다. **4~5 부분만 회귀, 분류에 따라 다르고 나머지 단계들은 공통**이다.

> 1\. 데이터 유형 파악  
> 2-1. 데이터 드랍 및 변수에 할당  
> 2-2. 전처리 (결측치, 이상치, 범주명 통일)  
>     - 수치형, 범주형 컬럼 리스트  
>     - 각 범주형 변수의 카테고리별 개수 및 훈련, 테스트 데이터 카테고리 종류 같은지 확인  
>     - 각 수치형 변수 이상치 확인 (하면 좋은데 이 부분은 사실상 안함 - 투머치라고 생각함)  
>     - (필요시) 결측값 처리  
> 2-3. 수치형 변수 스케일링  
> 2-4. 범주형 변수 인코딩  
> 3\. 데이터 분할  
> **4\. 모델 학습 -> (회귀/분류)에 따라 다름**  
> **5\. 평가 **\-> (회귀/분류)에 따라 다름****  
> 6\. 5의 결과, 성능 가장 좋은 모델로 테스트 데이터 결과 예측 및 저장  
> 7\. 저장한 결과 다시 불러와서 확인 (문제 조건에 따라 잘 저장했는지 확인)


<br>


**분류 & 회귀 공통 부분 (1-3, 6-7)**

```
import pandas as pd

### 1. 데이터 유형 파악
print(X_train.info())
print(y_train.info())
print(X_test.info())
print(y_test.info())
print(X_train.head())
print(y_train.head())


### 2-1. 데이터 할당 및 드랍
X_train = X_train.drop(["CustomerId", "Surname"], axis=1)
y_train = y_train.drop("CustomerId", axis=1)
X_test = X_test.drop(["CustomerId", "Surname"], axis=1)
y_test = y_test.drop("CustomerId", axis=1)
print(X_train.shape, y.shape, X_test.shape)


### 2-2. 전처리 (결측치, 이상치, 불균형, 범주명 조정)
# 수치형, 범주형 컬럼 리스트
not_num_col = X_train.select_dtypes(include="object").columns
num_col = X_train.select_dtypes(exclude="object").columns

# 각 범주형 변수의 카테고리별 개수 및 훈련, 테스트 데이터 카테고리 종류 같은지 확인
for not_num_col in not_num_cols:
    print()
    print(X_train[not_num_col].value_counts())
    print(set(X_train[not_num_col]), set(X_test[not_num_col]))
X_train["Gender"] = X_train["Gender"].apply(lambda x: x.lower().strip())
X_test["Gender"] = X_test["Gender"].apply(lambda x: x.lower().strip())

# (필요시) 각 수치형 변수 확인 및 정리 (이상치 제거, 결측치 처리)
X_train['결측치 열'] = X_train['결측치 열'].fillna(0)
X_test['결측치 열'] = X_test['결측치 열'].fillna(0)
print(X_train.isnull().sum())
print(X_test.isnull().sum())



### 2-3. 수치형 변수 스케일링
from sklearn.preprocessing import MinMaxScaler
scaler = MinMaxScaler()
X_train[num_col] = scaler.fit_transform(X_train[num_col])  # 스케일링
X_test[num_col] = scaler.transform(X_test[num_col])   # 스케일링 한 것을 적용


### 2-4. 모든 범주형 변수 인코딩
from sklearn.preprocessing import LabelEncode
for col in not_num_col:  # 각 카테고리 다를 때, 카테고리 개수 많은 걸로!
    encoder = LabelEncoder()
    X_train[col] = encoder.fit_transform(X_train[col])
    X_test[col] = encoder.transform(X_test[col])

y_encoding = False
if y_train.dtype == 'object':
    y_encoding = True
    y_encoder = LabelEncoder()
    y_train = y_encoder.fit_transform(y_train)


### 3. 데이터 분리
from sklearn.model_selection import tran_test_split
X_train, X_val, y_train, y_val = train_test_split(X_train, y, test_size=0.2)   # 🚨🚨🚨🚨 입력 및 출력 순서
print(X_train.shape, X_val.shape, y_train.shape, y_val.shape)

.
.
.

### 6. 5의 결과, 성능 가장 좋은 모델로 테스트 데이터 결과 예측 및 저장
y_pred = model.predict(X_test)
if y_encoding:   # 인코딩 되돌려서 저장
    y_pred = y_encoder.inverse_transform(random_pred)
result = pd.DataFrame(y_pred, columns=['pred'])
result.to_csv('result.csv', index=False)

rmse = round(np.sqrt(mean_squared_error(y_test, y_pred)), 3)
r2 = round(r2_score(y_test, y_pred), 3)
print("gbm", rmse, r2)


### 7. 생성 결과 확인
result = pd.read_csv('result.csv')
print(result)
print(y_test)
```


<br>


**회귀 (4~5)**

```
### 4. 모델 학습
from sklearn.ensemble import RandomForestRegressor
from xgboost import XGBRegressor
from lightgbm import LGBMRegressor
random_model = RandomForestRegressor()
random_model.fit(X_train, y_train)
random_pred = random_model.predict(X_val)

xgb_model = XGBRegressor()
xgb_model.fit(X_train, y_train)
xgb_pred = xgb_model.predict(X_val)

gbm_model = LGBMRegressor()
gbm_model.fit(X_train, y_train)
gbm_pred = gbm_model.predict(X_val)


### 5. 평가
from sklearn.metrics import mean_squared_error, r2_score, mean_absolute_error
import numpy as np
random_rmse = round(np.sqrt(mean_squared_error(y_val, random_pred)), 3)
random_r2 = round(r2_score(y_val, random_pred), 3)
random_mae = round(mean_absolute_error(y_val, random_pred), 3)
xgb_rmse = round(np.sqrt(mean_squared_error(y_val, xgb_pred)), 3)
xgb_r2 = round(r2_score(y_val, xgb_pred), 3)
xgb_mae = round(mean_absolute_error(y_val, random_pred), 3)
gbm_rmse = round(np.sqrt(mean_squared_error(y_val, gbm_pred)), 3)
gbm_r2 = round(r2_score(y_val, gbm_pred), 3)
gbm_mae = round(mean_absolute_error(y_val, random_pred), 3)
print("랜덤", random_rmse, random_r2)
print("xgb", xgb_rmse, xgb_r2)
print("gbm", gbm_rmse, gbm_r2)
```

<br>



**분류 (4~5)**

```
### 4. 모델 학습
from sklearn.ensemble import RandomForestClassifier
from xgboost import XGBClassifier
from sklearn.svm import SVC
random_model = RandomForestClassifier()
random_model.fit(X_train, y_train)
random_pred = random_model.predict(X_val)

xgb_model = XGBClassifier()
xgb_model.fit(X_train, y_train)
xgb_pred = xgb_model.predict(X_val)

svc_model = SVC()
svc_model.fit(X_train, y_train)
svc_pred = svc_model.predict(X_val)


### 5. 평가
from sklearn.metrics import roc_auc_score, accuracy_score
random_roc = roc_auc_score(y_val, random_pred)
random_acc = accuracy_score(y_val, random_pred)
xgb_roc = roc_auc_score(y_val, xgb_pred)
xgb_acc = accuracy_score(y_val, xgb_pred)
svc_roc = roc_auc_score(y_val, svc_pred)
svc_acc = accuracy_score(y_val,svc_pred)
print("랜덤:", random_roc, random_acc)
print("xgb:", xgb_roc, xgb_acc)
print("svc:", svc_roc, svc_acc)
```


<br>


---


<br>

